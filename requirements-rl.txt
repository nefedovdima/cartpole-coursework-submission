# E6a Linux CPU stack. Run with the project's Python 3.12, never system pip.
# Explicit +cpu prevents selecting PyPI's CUDA wheel.
--extra-index-url https://download.pytorch.org/whl/cpu
-r requirements-gymnasium.txt
torch==2.14.0+cpu
stable-baselines3==2.9.0
# Transitive CPU dependencies pinned from the reviewed resolver report.
Jinja2==3.1.6
MarkupSafe==3.0.3
filelock==3.32.6
fsspec==2026.7.0
mpmath==1.3.0
networkx==3.6.1
setuptools==84.0.0
sympy==1.14.0
