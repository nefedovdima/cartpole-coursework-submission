# Проверки воспроизводимости submission

Документ ведётся по редакциям. Ниже сначала идёт текущая редакция, затем —
исторические проверки прежних этапов, сохранённые дословно. Результаты разных
этапов не объединяются и один за другой не выдаются: каждая проверка относится
к тому комплекту, на котором выполнялась.

Содержание:

- [PUBLIC-редакция, 18.09.2026](#public-редакция-18092026) — текущая редакция;
- [Редакторская правка текста, 17.09.2026](#редакторская-правка-текста-17092026) — исторический раздел;
- [Предоставленная редакция с Times New Roman](#текущая-предоставленная-редакция-с-times-new-roman-17092026) — исторический раздел;
- [Исходная сборка](#исходная-сборка) — исторический раздел;
- [Локальная редакция до публикации](#локальная-редакция-до-публикации-17092026) — исторический раздел;
- [Редакция для PRIVATE-публикации](#редакция-для-private-публикации-17092026) — исторический раздел.

## PUBLIC-редакция, 18.09.2026

База: `3efaf85eee5b208bfc60239c66f2877d8dbbc9b1`. Изменены сведения о доступе
и разрешении на публикацию; в PDF — только заключительный абзац об электронных
материалах. Исторические разделы ниже и прежние записи PORTABILITY сохранены.
Прежний `current_pdf` целиком сохранён как `previous_private_editorial_pdf`.

Перед раскрытием проверены все 73 коммита публикуемой ветки, дополнительно —
прежний заменённый commit `65c200ad6ddc0d22d2b7075ab42ed6c5e3d69d7f`:
686 уникальных файловых объектов, текст 24 PDF, содержимое шести model ZIP
и одного NPZ. Установленного gitleaks/trufflehog не было; выполнены проверки
шаблонов ключей и токенов, присваиваний credentials, URL, локальных путей,
имён файлов и признаков диалогов с последующим разбором совпадений.
Реальных секретов, переписки, агентских промптов/логов и посторонних приватных
материалов не обнаружено. Два исторических локальных пути относятся к
лабораторным примерам upstream и не содержат чувствительных данных.
Проверка по шаблонам не является формальным доказательством отсутствия секретов.

Чистая сборка выполнена вне Git: XeLaTeX, три прохода, настоящий Times New Roman
во всех четырёх начертаниях доступен через fontconfig. **41 страница**;
обычное, полужирное и курсивное начертания Times New Roman встроены,
Liberation Serif отсутствует. Ноль Overfull hbox/vbox и Missing character;
оглавление стабильно. Все страницы отрендерены и просмотрены, последняя
страница и оглавление проверены отдельно. Страницы 1–40 побайтово совпадают
по текстовому слою и попиксельно при 90 dpi с предшествующим PDF; изменилась
только страница 41, сдвига остальных страниц нет.

Проверены 111 внутренних и 17 внешних ссылок-аннотаций, 39 пунктов оглавления
и все используемые именованные назначения; 150 действий GoTo корректны.
13 уникальных библиографических адресов сохранены. Ровно одна ссылка на
submission-репозиторий находится на последней странице. Launch, GoToR,
JavaScript, вложения и ссылки на локальные файлы отсутствуют.

Команды повторной проверки текущего комплекта:

```bash
git diff --check
sha256sum -c provenance/SHA256SUMS
python3 -B docs/report/scripts/check_report.py
python3 -B -m scripts.verify_submission
python3 -B -m scripts.reproduce_results --check
python3 -B -m scripts.reproduce_robustness --check
python3 -B -m cartpole.experiments.robustness_analysis --normalized results/robustness/normalized.json --check
python3 -B -m unittest tests.test_report_package tests.test_submission tests.test_robustness_results
```

Пройдены 12 групп сверок отчёта, 356 контрольных сумм, 212 относительных
ссылок и все 26 тестов отчёта, submission и компактных robustness-результатов
без пропусков. Компактные пересчёты main, Stage 1, confirmation и robustness
совпали с сохранёнными результатами; научный хеш normalized не изменился.

Научные проверки не ослаблены; в тестах не было требования сохранять PRIVATE.
Научный код, конфигурации, данные, числовые результаты, модели, изображения,
видео и `docs/AI_USE.md` не изменены. Обучение, rollout, inference,
новые оценки политик и полный raw-аудит не выполнялись.

Разрешение на публикацию зафиксировано в [THIRD_PARTY_NOTICES](../THIRD_PARTY_NOTICES.md).
PUBLIC-редакция предназначена для просмотра без авторизации. Техническое
открытие GitHub выполняется после успешных проверок и обычного push;
актуальная видимость определяется GitHub, а не историческими разделами ниже.

## Редакторская правка текста, 17.09.2026

Правились текст отчёта и пользовательская документация. Научный код,
конфигурации, данные, модели, изображения, принятые видео и исторические
протоколы сохранены побайтово. Рабочее дерево сверено с базовой редакцией
`dc5caf63f7336a74b5e34ff624f24ec71640c4ad` по содержимому объектов Git:
из 357 отслеживаемых файлов изменён 21 (документация, исходники отчёта, PDF
и `provenance/SHA256SUMS`), остальные 336 совпадают побайтово. Отдельно
пересчитаны 297 путей научных деревьев — `cartpole/`, `configs/`, `scripts/`,
`tests/`, `models/`, `media/`, `results/`, `docs/assets/`, рисунки, данные
и скрипты отчёта, исторические записи `provenance/`: расхождений нет, ни один
из них в diff не входит. Файлы не добавлялись и не удалялись. Новые обучение,
rollout, inference, проверка сырых журналов и перекодирование видео
не выполнялись.

**Сборка PDF выполнена локально.** Сначала во временной копии вне репозитория
собраны неизменённые исходники: результат совпал с опубликованным PDF
по числу страниц (36), набору встроенных шрифтов, числу внутренних переходов
и множеству внешних адресов. Единственное расхождение — переносы: в среде
этой сборки доступны русские шаблоны переносов, которых не было в среде
исходной сборки, поэтому при том же содержании неизменённых исходников
разбиение строк отличается. Побайтового совпадения не требовалось
и не достигалось. К новой редакции это сравнение не относится: её текст
правился, и прежнюю вёрстку она не воспроизводит.

Затем собрана новая редакция: XeLaTeX (TeX Live 2023), настоящий Times New Roman
из `msttcorefonts` во всех четырёх начертаниях, три прохода без ошибок.
Лежащий в дереве `docs/report/report.pdf` — прямой результат этой сборки,
выполненной во временной копии вне репозитория, чтобы не оставлять в Git
промежуточных файлов TeX. Повторный прогон на неизменных исходниках дал те же
41 страницу и побайтово совпадающий текстовый слой; расхождение байтов PDF
сводится к отметке времени сборки. Контрольные суммы PDF и всех исходников
отчёта в `PORTABILITY.json` пересчитаны и соответствуют файлам рабочего дерева.

Проверено на готовом PDF: **41 страница**; встроены `TimesNewRomanPSMT`,
`TimesNewRomanPS-BoldMT` и `TimesNewRomanPS-ItalicMT`, шрифт Liberation
отсутствует; 128 ссылок-аннотаций — 111 внутренних и 17 внешних; 39 закладок;
всего 150 переходов `/GoTo`, и все 84 использованных именованных назначения
определены в документе, недостижимых нет. Внешних адресов 14 уникальных:
13 библиографических и ровно одна ссылка на репозиторий на последней странице.
Действий `/Launch`, `/GoToR` и `/JavaScript`, вложенных файлов и ссылок
на локальные файлы в PDF нет. Все 39 строк оглавления ведут на страницы,
где соответствующий заголовок действительно находится.

Все 41 страница отрендерена и просмотрена: наложений, обрезанного текста
и пропавших символов нет, таблицы, подписи и формулы читаются. В журнале
сборки ноль `Overfull \hbox`, ноль `Overfull \vbox`, ноль `Underfull \vbox`
и ноль сообщений `Missing character`; 17 `Underfull \hbox` — это строки перед
выключными формулами и узкие ячейки таблиц, видимых дефектов они не дают.
В преамбулу добавлен запрет разрыва страницы после строки с переносом слова
и более свободные параметры размещения плавающих объектов. Проверка переносов
по извлечённому тексту всех 41 страницы: строк, которые заканчиваются частью
слова и продолжаются на следующей странице, не осталось ни одной — прежние
случаи `тележ-/ки`, `про-/двинулось` и `чи-/сел` устранены, и вариант
с целой страницей рисунка между частями слова больше не встречается.
Внутристрочные переносы сохранены как обычная типографика. Оставшиеся неполные
страницы — это концы глав, заключения, библиографии и коротких приложений,
где пустое место возникает из-за начала следующего раздела с новой страницы.
Ручное открытие внешних сайтов в пользовательском просмотрщике не проверялось.

Выполненные проверки:

```bash
python3 -B docs/report/scripts/check_report.py
python3 -B -m scripts.reproduce_results --check
python3 -B -m scripts.reproduce_robustness --check
python3 -B -m cartpole.experiments.robustness_analysis --normalized results/robustness/normalized.json --check
python3 -B -m scripts.verify_submission
python3 -B -m unittest tests.test_report_package tests.test_robustness_results
python3 -B -m unittest tests.test_cart_safety tests.test_robustness_analysis \
    tests.test_robustness_reporting tests.test_robustness_video
python3 -B -m unittest tests.test_submission tests.test_frozen_inference
```

Проверка отчёта прошла все 12 групп сверок. Пересчёт компактных результатов
подтвердил числа основной серии, ключи отбора Stage 1 и критерий confirmation;
пересчёт robustness — 98 заданий, 1960 эпизодов, 980 пар, 441/796 успехов,
585/0 превышений с допуском и научный хеш нормализованного набора.
`verify_submission` прошла: 356 сверенных сумм, 208 относительных ссылок,
шесть моделей и шесть видео на месте.

Тесты, выполнимые без научного окружения, пройдены: упаковка отчёта 13 из 13,
компактные robustness-результаты 3 из 3, защитный фильтр 24 из 24, анализ
robustness 56 из 56, видеопротокол 41 из 41, рендер-отчётность 27 из 29 — два
её теста пропущены самим набором как требующие Matplotlib. Всего по всем
запущенным наборам 175 тестов пройдены, 2 пропущены, ни один не провалился
по существу.

Часть тестов заблокирована отсутствующими научными зависимостями, и её
результаты успешными не объявляются. `tests.test_submission`: 5 из 10 пройдены
(состав и относительные ссылки, пересчёт компактных результатов, два теста
о недопустимости именованных сценариев в отборе и подтверждении, критерий
подтверждения), 5 завершились ошибками импорта `numpy` и `gymnasium`.
`tests.test_frozen_inference`: 6 из 9 пройдены, 3 завершились ошибкой импорта
`numpy`. Полностью заблокированы ошибкой импорта `numpy` наборы
`tests.test_constrained_simulator`, `tests.test_inference_compatibility`,
`tests.test_lqr_experiment`, `tests.test_pilot_reward`,
`tests.test_pydrake_simulator` и `tests.test_training_entrypoint`.
Причина одна: научное окружение (numpy, torch, Drake, Gymnasium,
Stable-Baselines3) в этой подготовке не устанавливалось. Это заблокированные
зависимости, а не отказ проверки: код тестов и научные модули побайтово
совпадают с базовой редакцией `dc5caf63f7336a74b5e34ff624f24ec71640c4ad`,
поэтому тот же набор даёт на ней те же ошибки импорта. Прежние результаты
этих тестов за текущую проверку не выдаются; загрузка моделей, вычисление проб
и запуск эпизодов не повторялись. Ни один тест не ослаблялся, не пропускался
и не правился ради прохождения.

Записи происхождения PDF дополнены, но не изменены: прежняя запись
`current_pdf` сохранена под именем `previous_methodical_pdf`, а записи
`historical_pdf`, `previous_editorial_pdf` и `previous_published_pdf`
остались прежними.

## Исторические проверки

Разделы ниже относятся к прежним этапам и сохранены без изменений.

## Текущая предоставленная редакция с Times New Roman, 17.09.2026

Проверены SHA входного ZIP и его безопасный состав: 357 файлов; относительно
опубликованной базы `5d705e89aada901025a4c7d7566e1c456e5104a0` изменены ровно
15 заявленных файлов, остальные 342 совпадают побайтово. Происхождение архива
зафиксировано в [provenance](../provenance/README.md). Дополнительно согласованы
актуальные указатели в документации; исторические записи ниже сохранены.

Повторно выполнены **12 групп** check_report и **17 целевых тестов**: 13
report_package, 3 robustness_results и одна проверка состава submission.
verify_submission проверяет 356 SHA, относительные Markdown-ссылки, синтаксис,
отсутствие секретов/личных путей и ограничение размеров. Это не повтор всего
научного тестового набора или нового inference.

```bash
python3 -B -S docs/report/scripts/check_report.py
python3 -B -S -m scripts.verify_submission
python3 -B -S -m unittest tests.test_report_package tests.test_robustness_results tests.test_submission.SubmissionTests.test_manifest_links_syntax_and_artifact_limits -v
```

Готовый PDF сохранён без пересборки: **36 страниц**, встроенные Times New Roman,
101 внутренний переход, 17 URI-аннотаций, из них ровно одна ссылка на submission
на последней странице. Множество библиографических адресов совпадает с предыдущим
PDF; Launch/GoToR отсутствуют. Все страницы отрендерены и визуально просмотрены.
Ручное открытие внешних сайтов и интерактивных ссылок в пользовательском
PDF-просмотрщике не проверялось. Times New Roman локально отсутствует, поэтому
пересборка этой редакции не проверена и другой шрифт не подставлялся.
Обучение, rollout, raw-аудит и перекодирование видео не запускались.

## Исходная сборка

Сборка `e53d39699dacd0490d875df8d2ec7642503f3081`: проверялся отдельный экспорт Git index в новую папку, без `.git`, исследовательской
venv и скрытых исходных данных. Зависимости установлены заново в отдельное окружение;
editable-пакет устанавливался именно из экспортированной копии. Импорты проверены
также из её родительской папки. `pip check`: No broken requirements found.

| Проверка | Фактический результат |
|---|---|
| Анализатор robustness | 56 тестов, синтетические compact/raw fixtures |
| Reporting | 29 тестов |
| Видеорендерер | 41 тест, без перекодирования принятого комплекта |
| Safety-математика | 24 теста |
| Inference compatibility | 14 тестов |
| Submission | 10 тестов |
| Inference-комплекты | 9 тестов: порча SHA, недостающие/лишние файлы, routing mock |
| Компактные результаты | 3 теста |
| Training entrypoint | 3 теста только конфигурации/отказ без guard |
| **Всего** | **189/189, OK, 116.268 с**, без пропусков |
| Main/Stage1/confirmation | Счётчики 33 обучений, validation20 tuples и отрицательные gates подтверждены compact CSV |
| Robustness | 1960/980; шесть CSV заново вычислены побайтово |
| Графики | Пять PNG + пять SVG пересозданы из normalized и побайтово совпали |
| Загрузка весов CPU | 6/6; по 23 observations, две формы запросов, всего 276 сравнений; max abs difference=0 |
| Report checks | 11 групп сверок данных/ссылок/чисел, без новой сборки PDF |
| Сохранность | 1065 исходных файлов research неизменны; 61 Python-файл cartpole идентичен источнику |
| Происхождение | Исторические manifests сохранены; семь переносимых Markdown-производных явно отмечены |
| Состав | Проверены SHA всех payloads, относительные Markdown-ссылки, AST, секреты/личные пути, размеры |

Команда целевых тестов (не полный discovery, который включает симуляционные тесты):

```bash
MPLBACKEND=Agg .venv/bin/python -B -m unittest \
 tests.test_robustness_analysis tests.test_robustness_reporting tests.test_robustness_video \
 tests.test_cart_safety tests.test_inference_compatibility tests.test_submission \
 tests.test_robustness_results tests.test_frozen_inference tests.test_training_entrypoint -v
```

[Историческая машинная квитанция этой сборки](../provenance/submission_checks.json) и
[проверка inference](../provenance/inference_check.json) не содержат локальных путей.
[Актуальный список SHA](../provenance/SHA256SUMS). Число файлов/ссылок берётся из
`python3 -B -m scripts.verify_submission`, чтобы не дублировать изменчивый счётчик.

Scientific Python-пакет и frozen configs не менялись. Только submission wrappers,
документация и проверка переносимых описаний добавлены вокруг исходных интерфейсов.
CPU: Python3.12.3, torch2.14.0+cpu, SB3/sb3-contrib2.9.0, Drake1.57.0,
NumPy2.5.3, Gymnasium1.3.0, Matplotlib3.11.2; полные pins — в requirements.
Проверка нормализованных данных дополнительно прошла с `python3 -S` (без site-packages).

**Не выполнялись:** обучение, новые policy evaluation/Drake rollout, сервер,
CUDA, final5000, полный raw-аудит, новое кодирование шести MP4, пересборка PDF.
Evaluation wrapper проверен планом и mock; его новые эпизоды не являются измеренным
результатом. Загрузка весов и вычисление сохранённых probe actions — реальные проверки.
Историческая page-by-page QA отчёта и видео перенесена как источник, а не выдана
за повторную ручную проверку. Интерактивные PDF/видеоплееры заново не проверялись.

Отсутствие лицензии upstream не устранено техническими тестами. Публикация и
окончательное авторское согласование отчёта остаются отдельными решениями.

## Локальная редакция до публикации, 17.09.2026

Исторические 189 тестов выше не выдаются за повторный запуск. После редакторских
исправлений отдельно проверены текущая и прежняя идентичности PDF, исходники
вёрстки, компактные научные таблицы и состав комплекта. Научный код, данные,
модели, изображения и принятые видео сохранены побайтово.

Эта редакция PDF пересобрана офлайн: **36 страниц**, все страницы отрендерены и визуально
просмотрены. Шрифты встроены; основной — Liberation Serif, поскольку Times New Roman
отсутствует. Проверены 51 локальная ссылка PDF (31 различный файл), 17 внешних ссылок
по структуре адреса и внутренние переходы. Проверка не означает ручного открытия
всех внешних сайтов или воспроизведения видео в PDF-просмотрщике.

Короткий набор для этой редакции:

```bash
python3 -B docs/report/scripts/check_report.py
python3 -B -m scripts.reproduce_results --check
python3 -B -m scripts.reproduce_robustness --check
python3 -B -m scripts.verify_submission
MPLBACKEND=Agg .venv/bin/python -B -m unittest \
 tests.test_report_package tests.test_submission tests.test_robustness_results -v
```

На момент этой проверки набор содержал 23 теста; новая десятка проверяет разделение прежнего и текущего PDF,
защиту исходников и данных, локальные ссылки и отказ для неполной/фиктивной
настройки публикации. Проверка отчёта содержит 12 групп сверок. Чистый раздаточный
ZIP проверяется после распаковки по SHA, относительным ссылкам, компактным данным
и десяти тестам упаковки отчёта; научное окружение для этих проверок не требуется.
Обучение, испытания политик, raw-аудит и перекодирование видео не повторяются.

## Редакция для PRIVATE-публикации, 17.09.2026

PDF содержит 36 страниц и одну ссылку на главную страницу submission-репозитория.
Локальные Launch/GoToR-ссылки отсутствуют; библиография и внутренние переходы
сохранены. Все страницы отрендерены и визуально просмотрены. Проверка упаковки
проверяет новый контракт ссылок, прежние SHA источников и актуальную идентичность PDF.
Целевой набор включает 26 тестов (13 упаковки отчёта, 10 submission, 3 compact results).
Команды из предыдущего раздела остаются применимыми. Проверки научного окружения
используют ранее установленный CPU-стек; новых загрузок моделей и эпизодов нет.

В чистом клоне доступны без site-packages: verify_submission, reproduce_results,
reproduce_robustness, check_report и 13 тестов report_package. Эти проверки сверяют
состав, относительные ссылки, 6 моделей по SHA, 6 видео/постеров, компактные числа
и исходники отчёта. Проверка наличия файлов не равна повторной проверке inference.
Видеогалерея добавлена отдельно; исторические media README/manifest/QA не изменены.
Чистый ZIP содержит тот же проверенный payload, без Git, окружений и логов.
Доступ комиссии к PRIVATE-репозиторию не подтверждён этой технической проверкой.

## Уточнение описания применения генеративных моделей, 18.09.2026

Изменён только `docs/AI_USE.md`: описание участия генеративных моделей
приведено в соответствие с фактическим объёмом помощи. Контрольная сумма
файла в `provenance/SHA256SUMS` пересчитана. Научный код, конфигурации,
данные, числовые результаты, модели, изображения и видео не изменялись;
обучение, rollout и inference не выполнялись.
