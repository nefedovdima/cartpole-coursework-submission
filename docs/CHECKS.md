# Проверки submission от16.09.2026

Проверки выполнены локально, без обучения, фактических rollout и обращения к
вычислительному серверу. Источники проекта и исходного отчёта не изменены.

| Проверка | Фактический результат |
| --- | --- |
| Установка из чистого Git-клона, новая Python3.12.3 venv | requirements и editable-пакет установлены успешно |
| pip check | No broken requirements found |
| test_submission.py | 10/10 |
| test_cart_safety.py | 24/24; аналитические проверки, без Drake |
| test_inference_compatibility.py | 14/14; синтетическая модель и mocked loader |
| Импорты и check_stack | пройдены, все25 source hashes контракта сохранены |
| План штатной оценки | 23 эпизода=20 validation+3 named,10с; execute=false |
| Числа из compact CSV | main successes, Stage1 tuples и confirmation gate пересчитаны |
| Исходные файлы материалов отчёта | 58/58 SHA совпали до переноса |
| Перенесённые файлы | 156/156 импортированных SHA совпадают |
| Markdown | 60 относительных ссылок проверено; адреса не зависят от прежней машины |
| PDF | 12 страниц A4;23 относительные ссылки разрешаются; страницы просмотрены |
| MP4 | 4/4 полностью декодируются;251 кадр,25fps,10.04с; выбранные кадры просмотрены |
| Состав Git | нет моделей, replay/runtime, raw, архивов, секретов, личных абсолютных путей |
| Размеры | нет файлов больше50MB; LFS/Release для текущего состава не нужны |
| Исходные каталоги | 847 tracked research и118 файлов отчёта сверены по SHA, без изменений |
| Git | лабораторная история сохранена; diff --check проходит |

Итого **48 целевых тестов** в новом окружении. Установленные основные версии:
Drake1.57.0, NumPy2.5.3, Gymnasium1.3.0, Matplotlib3.11.2,
PyTorch2.14.0+cpu, Stable-Baselines3/sb3-contrib2.9.0.
Пакет импортируется из чистого клона, не из исследовательского каталога.

SVG помечены как generated/binary diff, CSV сохраняют исходный CRLF через
.gitattributes: это сохраняет аудированные SHA вместо переформатирования данных.
У PDF нет Overfull boxes, пропавших символов или undefined references;
есть предупреждения Underfull в библиографии, без видимого нарушения верстки.

Пересчёт из compact CSV не является новым аудитом полных episode logs. Checkpoints
в этом клоне не загружались, обучение и evaluation rollout не выполнялись.
CUDA, реальная установка и интерактивные переходы по PDF-ссылкам не проверялись.
Проверка кадров и полного декодирования MP4 не означает ручное воспроизведение
во всех проигрывателях. LaTeX собран имеющимся Tectonic0.17.0 с отдельным cache;
TeX-инструменты не входят в Python-установку.

Полные журналы установки, тестов и локальный аудит текстовых совпадений сохранены
отдельно от submission. Публикация на GitHub ещё не выполнялась.
